spring:
  application:
    name: algoga_server

  security:
    oauth2:
      client:
        registration:
          google:
            client-id: ${GOOGLE_CLIENT_ID}
            client-secret: ${GOOGLE_CLIENT_SECRET}
            scope:
              - profile
              - email

  data:
    redis:
      host: ${REDIS_HOST}
      port: ${REDIS_PORT}

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: ${DB_URL}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}

  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true

  servlet:
    multipart:
      max-file-size: 500MB
      max-request-size: 500MB

  config:
    import:
      - optional:classpath:/domains/banner.yml
      - optional:classpath:/domains/userProfile.yml

  mail:
    host: smtp.gmail.com
    port: 587
    username: ${GMAIL_USERNAME}
    password: ${GMAIL_PASSWORD}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

  ai:
    openai:
      base-url: https://api.groq.com/openai
      api-key: ${LLM_KEY}
      chat:
        options:
          model: openai/gpt-oss-20b # 챗봇 답변용 모델
          temperature: 0.7

    ollama:
      base-url: http://localhost:11434
      chat:
        enabled: false
      embedding:
        options:
          model: bge-m3 # 벡터 필터링용 모델

server:
  port: 15000
  forward-headers-strategy: native

logging:
  level:
    org.hibernate.SQL: debug

springdoc:
  default-produces-media-type: application/json

jwt:
  secret-key: ${JWT_SECRET_KEY}
  access-token-expiration: 1800000 # 30분
  refresh-token-expiration: 604800000 # 7일

app:
  file:
    upload-root: uploads

portone:
  base-url: ${PORTONE_BASE_URL}
  store-id: ${PORTONE_STORE_ID}
  api-secret: ${PORTONE_API_SECRET}
  channel-key: ${PORTONE_CHANNEL_KEY}

cloud:
  aws:
    s3:
      endpoint: ${S3_ENDPOINT}
    credentials:
      access-key: ${AWS_ACCESS_KEY}
      secret-key: ${AWS_SECRET_KEY}
    region:
      static: ap-northeast-2

flight:
  api:
    base-url: ${FLIGHT_BASE_URL}
    service-key: ${FLIGHT_API_SERVICE_KEY}

user:
  storage:
    bucket: algoga-banner
    directory: profiles

  app:
    frontend:
      base-url: ${FRONTEND_URL:http://localhost:17000}
management:
  endpoints:
    web:
      exposure:
        include: health, info, prometheus, metrics, circuitbreakers
  endpoint:
    health:
      show-details: always
    prometheus:
      enabled: true
  metrics:
    tags:
      application: algoga-server
    distribution:
      percentiles-histogram:
        http.server.requests: true
      percentiles:
        http.server.requests: 0.5, 0.9, 0.95, 0.99

resilience4j:
  circuitbreaker:
    configs:
      default:
        registerHealthIndicator: true
        slidingWindowSize: 10 # 10번의 호출을 기준으로 통계 산출
        minimumNumberOfCalls: 5 # 최소 5번 호출 후부터 실패율 계산
        permittedNumberOfCallsInHalfOpenState: 3 # 반열림 상태에서 허용할 테스트 호출 수
        waitDurationInOpenState: 10s # 서킷이 열린 후 10초 대기 후 반열림 상태로 전환
        failureRateThreshold: 50 # 실패율 50% 이상이면 서킷 오픈 (호출 차단)
    instances:
      groqLlmApi:
        baseConfig: default
      portone:
        # CLOSED → OPEN: 10번 호출 중 50% 이상 실패 시
        sliding-window-type: COUNT_BASED
        sliding-window-size: 10
        failure-rate-threshold: 50
        # OPEN 상태 유지 시간 (10초 후 HALF_OPEN 전환)
        wait-duration-in-open-state: 10s
        # HALF_OPEN 상태에서 허용할 테스트 호출 수
        permitted-number-of-calls-in-half-open-state: 3
        # 예외 기록 대상: BusinessException은 제외 (PortOne이 정상 응답한 것)
        ignore-exceptions:
          - com.kidmily.algoga_server.global.exception.BusinessException
        register-health-indicator: true
      flight:
        sliding-window-type: COUNT_BASED
        sliding-window-size: 10
        failure-rate-threshold: 50
        wait-duration-in-open-state: 30s  # 공공 API라 복구 시간 더 여유있게
        permitted-number-of-calls-in-half-open-state: 3
        ignore-exceptions:
          - com.kidmily.algoga_server.global.exception.BusinessException
        register-health-indicator: true
  timelimiter:
    instances:
      portone:
        # PortOne API 타임아웃: 5초
        timeout-duration: 5s
        cancel-running-future: true
      flight:
        # 항공편 API 타임아웃: 10초 (공공데이터 API라 느릴 수 있음)
        timeout-duration: 10s
        cancel-running-future: true
